export interface MethodStep { number: string; title: string; description: string; }
export interface Brand { name: string; imageUrl?: string; }
export interface Testimonial { id: number; author: string; role: string; text: string; }
export interface SocialLink { label: string; url: string; }
export interface ContactConfig { whatsappNumber: string; whatsappMessage: string; email: string; }
export interface WorkPreview { label: string; accent: string; }
